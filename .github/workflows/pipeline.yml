# name: Node CI

# #event
# on:
#     push:
#         branches: [dev]
#     pull_request:
#         branches: [dev]
# jobs:
#     build:
#         runs-on: ubuntu-latest

#         strategy:
#             matrix:
#                 node-version: [16.x]

#         steps:
#             - name: Checkout the repo 
#             uses: actions/checkout@v2
#             - name: Build the Docker image
#             run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
#     # write deploy flow here
#     #deploy:
#     #    runs-on: ubuntu-latest
#     #    needs: build
