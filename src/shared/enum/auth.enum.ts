const EAdminPermissions = {
    LOGIN: 'LOGIN:ADMIN',
}

const EClientPermissions = {
    LOGIN: 'LOGIN:CLIENT',
}

const EAuth = {
    EAdminPermissions,
    EClientPermissions,
}

export default EAuth
